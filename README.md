# Prompt Mixer 🍌🏛️

A **static** prompt picker/editor for architectural AIGC.

## Changes in this version (per your notes)
- Summary is **always synced** (no toggle)
- Removed the two helper explanations under headings
- **No Convert button**: outputs update live, **Save** snapshots to history
- +Category moved into the **Pick options** header (top-right)
- Outputs: no "Nothing selected" placeholders
- Cleaner layout: no big left/right frames, no per-category outer backgrounds
- Center divider is **draggable** (default left 2 / right 3)

## Run locally
Open `index.html`, or use VSCode Live Server.

## Deploy to GitHub Pages
Upload these files to repo root:
- `index.html`
- `styles.css`
- `app.js`
- `defaults.json`

Enable Pages: Settings → Pages → Deploy from a branch → `main` / root.
